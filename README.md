# 🏆 Наследники Победы

Веб-платформа для проведения регионального творческого конкурса среди школьников Севастополя.

**Заказчик:** Севастопольское отделение «Боевого Братства»

---

## 📋 О проекте

Платформа обеспечивает приём конкурсных работ (сочинения и рисунки) в электронном виде, их распределение между экспертами и процедуру оценивания.

### Номинации

- **Великая Отечественная война** — Герои ВОВ (прадеды)
- **Специальная военная операция** — Герои СВО (современники)

### Форматы работ

- 📝 Сочинение (doc, docx, pdf)
- 🎨 Рисунок (jpg, png, jpeg)

---

## 🚀 Быстрый старт

### Требования

- Node.js 20+
- Docker и Docker Compose
- npm или yarn

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd nasledniki-pobedy
```

### 2. Запуск инфраструктуры (PostgreSQL, MinIO, MailHog)

```bash
docker-compose up -d
```

Это запустит:
- **PostgreSQL** — порт 5432
- **MinIO** (S3) — порт 9000 (API), 9001 (Console)
- **MailHog** — порт 8025 (Web UI)

### 3. Настройка Backend

```bash
cd backend

# Установка зависимостей
npm install

# Копирование .env
cp .env.example .env

# Применение миграций БД
npx prisma migrate dev

# Генерация Prisma Client
npx prisma generate

# Создание первого администратора (seed)
npm run seed

# Запуск в dev-режиме
npm run start:dev
```

Backend API будет доступен на: http://localhost:3000/api
Swagger документация: http://localhost:3000/api/docs

### 4. Настройка Frontend

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск в dev-режиме
npm run dev
```

Frontend будет доступен на: http://localhost:5173

---

## 🔗 Ссылки для локальной разработки

| Сервис | URL | Описание |
|--------|-----|----------|
| 🌐 Frontend | http://localhost:5173 | Веб-интерфейс |
| 🔧 Backend API | http://localhost:3000/api | REST API |
| 📖 Swagger | http://localhost:3000/api/docs | API документация |
| 📧 MailHog | http://localhost:8025 | Просмотр email |
| 📦 MinIO Console | http://localhost:9001 | S3 хранилище |

### Тестовые учётные данные

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | admin@nasledniki-pobedy.ru | admin123 |

⚠️ **Рекомендуется сменить пароль после первого входа!**

---

## 🏗️ Структура проекта

```
/
├── backend/                  # NestJS Backend
│   ├── src/
│   │   ├── auth/             # Аутентификация (JWT)
│   │   ├── users/            # Управление пользователями
│   │   ├── works/            # Конкурсные работы
│   │   ├── ratings/          # Оценки
│   │   ├── admin/            # Административные функции
│   │   ├── settings/         # Настройки системы
│   │   ├── mail/             # Email-уведомления
│   │   ├── prisma/           # Prisma ORM
│   │   └── common/           # Guards, Decorators, Pipes
│   └── prisma/
│       └── schema.prisma     # Схема БД
│
├── frontend/                 # React + Vite Frontend
│   ├── src/
│   │   ├── components/       # UI-компоненты
│   │   ├── pages/            # Страницы
│   │   ├── hooks/            # Кастомные хуки
│   │   ├── api/              # API-клиент
│   │   ├── store/            # Zustand state
│   │   └── utils/            # Утилиты
│   └── tailwind.config.js
│
├── docs/                     # Документация
│   ├── architecture.png      # Архитектурная схема
│   └── er-diagram.png        # ER-диаграмма БД
│
└── docker-compose.yml        # Локальная инфраструктура
```

---

## 🛠️ Технологический стек

### Backend
- **Framework:** NestJS
- **ORM:** Prisma
- **Database:** PostgreSQL
- **Auth:** JWT (access + refresh tokens)
- **Storage:** S3 (Timeweb Object Storage)
- **Email:** Nodemailer
- **Docs:** Swagger/OpenAPI

### Frontend
- **Framework:** React 18 + TypeScript
- **Build:** Vite
- **Styling:** Tailwind CSS
- **State:** Zustand
- **Forms:** React Hook Form + Zod
- **HTTP:** Axios
- **Animations:** Framer Motion

---

## 🧪 Тестирование

### Backend (Unit-тесты)

```bash
cd backend
npm test
```

Тесты покрывают:
- Модуль аутентификации (регистрация, вход, сброс пароля)
- Модуль работ (подача, редактирование, удаление)

### Frontend (E2E-тесты с Playwright)

```bash
cd frontend

# Запустить тесты (headless)
npm run test:e2e

# Запустить с UI для отладки
npm run test:e2e:ui

# Запустить с видимым браузером
npm run test:e2e:headed
```

⚠️ Перед запуском E2E-тестов убедитесь, что backend работает на порту 3000.

---

## 📹 Видео-демонстрация

Сценарий для записи видео-демонстрации системы находится в файле:

📄 [`docs/VIDEO_SCENARIO.md`](docs/VIDEO_SCENARIO.md)

Включает пошаговые инструкции для демонстрации всех функций системы (~6 минут).

---

## 📱 Пользовательские роли

### 👨‍🎓 Участник (Школьник)
- Регистрация и вход
- Подача работ (до 2 шт. — по одной на номинацию)
- Отслеживание статуса заявки
- Просмотр оценки

### 👨‍🏫 Эксперт
- Вход по логину/паролю от администратора
- Просмотр назначенных работ
- Оценка работ (балл + комментарий)

### 👨‍💼 Администратор
- Управление пользователями
- Создание экспертов
- Распределение работ
- Настройки (дедлайн, шкала оценки)
- Экспорт результатов в Excel

---

## 🔒 Безопасность

- **Rate Limiting** — защита от brute-force атак:
  - Регистрация: 5 запросов/мин
  - Вход: 10 запросов/мин
  - Сброс пароля: 3 запроса/мин
- **JWT-аутентификация** — access token (15 мин) + refresh token (7 дней)
- **Хеширование паролей** — bcrypt с 12 раундами
- **Валидация файлов** — проверка MIME-типа и размера (до 15 МБ)
- **Согласие ПДн** — обязательное при регистрации

---

## 🔐 Переменные окружения

### Backend (.env)

```env
# База данных
DATABASE_URL="postgresql://user:pass@host:5432/db"

# JWT
JWT_SECRET="your-secret"
JWT_REFRESH_SECRET="your-refresh-secret"

# S3
S3_ENDPOINT="https://s3.timeweb.cloud"
S3_BUCKET="nasledniki"
S3_ACCESS_KEY="xxx"
S3_SECRET_KEY="xxx"

# Email
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="xxx"
SMTP_PASS="xxx"

# Приложение
PORT=3000
FRONTEND_URL="https://nasledniki.example.com"

# Первый админ
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="xxx"
```

### Frontend (.env)

```env
VITE_API_URL="https://api.nasledniki.example.com"
```

---

## 🚢 Деплой на Timeweb Cloud

### Репозиторий

**GitHub:** https://github.com/rebekaee1/boevoebratstvo

**Ветки для деплоя:**
- `deploy-backend` — backend (NestJS) с Dockerfile в корне
- `deploy-frontend` — frontend (React) с Dockerfile в корне

### Быстрый старт

📖 **Пошаговая инструкция:** [docs/TIMEWEB_QUICKSTART.md](docs/TIMEWEB_QUICKSTART.md)

**Кратко:**

1. **Создайте сервисы в Timeweb Cloud:**
   - PostgreSQL (Managed DB)
   - S3 Storage (bucket: `nasledniki`)
   - Почта для домена `наследникпобеды.рф`

2. **Деплой Backend:**
   - App Platform → Dockerfile
   - Репозиторий: `rebekaee1/boevoebratstvo`
   - Ветка: `deploy-backend`
   - Добавьте переменные окружения (см. `backend/.env.example`)

3. **Деплой Frontend:**
   - App Platform → Dockerfile
   - Репозиторий: `rebekaee1/boevoebratstvo`
   - Ветка: `deploy-frontend`
   - Build Argument: `VITE_API_URL=https://ваш-backend-домен/api`

4. **Привяжите домен:**
   - `наследникпобеды.рф` → frontend
   - `api.наследникпобеды.рф` → backend

**Полная документация:** [docs/DEPLOY_TIMEWEB.md](docs/DEPLOY_TIMEWEB.md)

**Стоимость:** ~1625 ₽/месяц

---

## 📄 Лицензия

Проект разработан для Севастопольского отделения «Боевого Братства».

---

## 📞 Контакты

**Севастопольское отделение «Боевого Братства»**
