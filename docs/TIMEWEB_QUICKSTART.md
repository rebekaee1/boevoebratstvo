# Timeweb Cloud — Быстрый старт

## Репозиторий готов! 🎉

**GitHub:** https://github.com/rebekaee1/boevoebratstvo

---

## Пошаговый план деплоя

### 1️⃣ Создайте сервисы

| Сервис | Где создать | Стоимость |
|--------|-------------|-----------|
| **PostgreSQL** | Облачные базы данных → PostgreSQL | ~790 ₽/мес |
| **S3 Storage** | Хранилище S3 → Создать бакет | ~25 ₽/мес |
| **Почта** | Домены и SSL → наследникпобеды.рф → Почта | Бесплатно |

---

### 2️⃣ Деплой Backend

**App Platform → Создать приложение:**

1. **Тип:** Docker → Dockerfile
2. **Репозиторий:** `rebekaee1/boevoebratstvo`
3. **⚠️ ВЕТКА:** `deploy-backend` (не main!)
4. **Коммит:** ✅ Сборка по последнему коммиту
5. **Регион:** Санкт-Петербург
6. **Конфигурация:** 1 × 3.3 ГГц, 2 ГБ RAM, 30 ГБ
7. **Переменные окружения** — добавьте ВСЕ переменные из `backend/.env.example`:

```env
DATABASE_URL=postgresql://user:pass@host:5432/nasledniki?schema=public
JWT_SECRET=сгенерируйте-через-openssl-rand-hex-32
JWT_REFRESH_SECRET=другой-ключ-openssl-rand-hex-32
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
S3_ENDPOINT=https://s3.timeweb.cloud
S3_BUCKET=nasledniki
S3_ACCESS_KEY=ваш-s3-ключ
S3_SECRET_KEY=ваш-s3-секрет
S3_REGION=ru-1
SMTP_HOST=smtp.timeweb.ru
SMTP_PORT=465
SMTP_USER=noreply@наследникпобеды.рф
SMTP_PASS=пароль-от-ящика
SMTP_FROM=Наследники Победы <noreply@наследникпобеды.рф>
PORT=3000
FRONTEND_URL=https://наследникпобеды.рф
ADMIN_EMAIL=admin@наследникпобеды.рф
ADMIN_PASSWORD=ваш-надежный-пароль
```

8. **Имя приложения:** `nasledniki-backend`
9. **Запустить деплой**

Дождитесь завершения (~5-7 минут). Запишите **технический домен** (например: `app-12345.timeweb-apps.com`).

---

### 3️⃣ Деплой Frontend

**App Platform → Создать приложение:**

1. **Тип:** Docker → Dockerfile
2. **Репозиторий:** `rebekaee1/boevoebratstvo`
3. **⚠️ ВЕТКА:** `deploy-frontend` (не main!)
4. **Коммит:** ✅ Сборка по последнему коммиту
5. **Регион:** Санкт-Петербург
6. **Конфигурация:** 1 × 3.3 ГГц, 1 ГБ RAM, 15 ГБ (можно минимальный — frontend статика)
7. **Переменные сборки (Build Arguments):**
   
   | Имя | Значение |
   |-----|----------|
   | `VITE_API_URL` | `https://ВАШ-BACKEND-ДОМЕН/api` |

   > Используйте технический домен backend из предыдущего шага.
   > Позже замените на: `https://api.наследникпобеды.рф/api`

8. **Имя приложения:** `nasledniki-frontend`
9. **Запустить деплой**

Дождитесь завершения (~3-5 минут).

---

### 4️⃣ Привяжите домен

#### Frontend (главный домен):

1. **Домены и SSL** → **Добавить домен** → `наследникпобеды.рф`
2. **Привязать к сервису:** выберите `nasledniki-frontend`
3. У регистратора домена пропишите **NS-серверы Timeweb:**
   ```
   ns1.timeweb.ru
   ns2.timeweb.ru
   ns3.timeweb.org
   ns4.timeweb.org
   ```
4. Ждите обновления DNS (2-24 часа)
5. SSL выпустится **автоматически**

#### Backend API (субдомен):

1. **Домены и SSL** → `наследникпобеды.рф` → **DNS**
2. Добавьте **A-запись:**
   - Поддомен: `api`
   - Привязать к сервису: `nasledniki-backend`

---

### 5️⃣ Финальная настройка

После привязки домена **обновите переменные:**

**Backend (`nasledniki-backend`):**
- `FRONTEND_URL` → `https://наследникпобеды.рф`

**Frontend (`nasledniki-frontend`):**
- Build Argument `VITE_API_URL` → `https://api.наследникпобеды.рф/api`

Перезапустите деплой обоих приложений.

---

## ✅ Проверка

После деплоя проверьте:

- [ ] https://наследникпобеды.рф — главная страница
- [ ] https://api.наследникпобеды.рф/api — Hello World!
- [ ] https://api.наследникпобеды.рф/api/docs — Swagger UI
- [ ] Регистрация нового участника
- [ ] Email приходит на почту
- [ ] Вход администратора
- [ ] Загрузка файла работы

---

## 📞 Поддержка

Если возникнут проблемы:
- **Логи:** App Platform → ваше приложение → Логи приложения
- **Поддержка Timeweb:** 24/7, ответ в течение 15 минут
- **Документация:** https://timeweb.cloud/docs/apps

---

## 💰 Итоговая стоимость

| Сервис | Стоимость |
|--------|-----------|
| Backend App | ~810 ₽/мес |
| Frontend App | Бесплатно* |
| PostgreSQL | ~790 ₽/мес |
| S3 Storage | ~25 ₽/мес |
| **Итого** | **~1625 ₽/мес** |

\* Frontend-приложения на Timeweb бесплатны до 100к запросов/мес

---

*Проект готов к деплою! Успешного запуска! 🚀*
